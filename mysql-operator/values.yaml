replicaCount: 3

image:
  repository: mariadb
  tag: "11.4"
  pullPolicy: IfNotPresent

mysql:
  rootPassword: "changeme123"
  database: "learning_hub"
  user: "appuser"
  password: "apppass123"

persistence:
  enabled: true
  storageClass: "local-path"
  size: 10Gi

resources:
  limits:
    cpu: 500m
    memory: 1Gi
  requests:
    cpu: 250m
    memory: 512Mi

service:
  type: ClusterIP
  port: 3306
